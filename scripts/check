#!/usr/bin/env bash
set -euo pipefail

uv sync --dev --locked
uv run ruff check .
uv run pyrefly check
uv run pytest -q

bun install --frozen-lockfile
bun run gtrade:typecheck
bun run gtrade:test
bun run ostium:typecheck
bun run ostium:test
