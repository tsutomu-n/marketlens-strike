# Data Snapshot Manifest

DataSnapshotManifest fixes the exact data used by a strategy experiment. It must include paths, hashes, symbol universe, venue list, min/max timestamps, phase gate summary, and data quality summary. Trials without a data_snapshot_id must fail.
