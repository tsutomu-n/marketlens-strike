# Codex Prompt PR-MLS-SL4

あなたは `marketlens-strike` の実装者です。このPRは Strategy Research Lab migration の `PR-MLS-SL4` です。

必ず守ること:

```text
- production live tradingを実装しない
- wallet/signingを実装しない
- public micro live CLIを出さない
- OrderIntentという名前をresearch/paper preview層で使わない
- PaperIntentPreviewはlive変換禁止
- ./scripts/checkを最後に通す
```

資料パックの `prs/` にある対応PR文書を読んでから実装してください。
